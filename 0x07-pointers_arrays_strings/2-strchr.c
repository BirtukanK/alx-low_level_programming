#include "main.h"
#include <string.h>
/**
 * _strchr - a function that locates a character in a string
 * @s: the string from which we locate a characte
 * @c: character to locate
 * Return: 0 or 1
 */
char *_strchr(char *s, char c)
{
return (strchr(s, c));
}
