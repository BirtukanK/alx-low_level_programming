#include <stdio.h>
/**
 * mul - a function that multiplies two integers
 * @a: first number to multiply
 * @b: second number to multiply
 * Return: result value
 */
int mul(int a, int b)
{
return (a * b);
}
