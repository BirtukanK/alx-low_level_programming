#include <ctype.h>
/**
 * _isupper - a function which checks uppercase
 * @c: character to check if it is upper or not
 * Return: 0 or 1
 */
int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
else
{
return (0);
}
}
