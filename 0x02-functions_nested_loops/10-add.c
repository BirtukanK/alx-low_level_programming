#include "main.h"
/**
 * add - returns the sum of its parameters
 * @a: int type number
 * @b: int type number
 * Return:0
 */
int add(int a, int b)
{
int result;
result = a + b;
_putchar(result);
return (0);
}
